class UserDomain {
    constructor(id, name, lastname, createdAt, updatedAt) {
        this.id = id
        this.name = name
        this.lastname = lastname
        this.createdAt = createdAt
        this.updatedAt = updatedAt
    }
}

export default UserDomain